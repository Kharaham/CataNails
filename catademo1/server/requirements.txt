fastapi>=0.110,<0.112
pydantic>=2.5,<3
uvicorn[standard]>=0.27,<0.30
python-multipart
pillow
numpy
opencv-contrib-python
mediapipe
scikit-image
# Opcional (solo si usarás SAM en Cloud Run CPU/GPU):
# torch
# torchvision
# segment-anything
# firebase_admin   # habilita verificación de ID tokens si pones FIREBASE_AUTH_REQUIRED=true
